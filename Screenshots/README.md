# Screenshots

Streamlit UI captures referenced from the root `README.md`. Drop the canonical three into this folder once recorded.

| Filename | Screen |
|---|---|
| `01_chat_ui.png` | Streamlit chat UI — sidebar examples + answer with confidence badge |
| `02_tool_expansion.png` | Tool-call data expanded under an answer (compare_usage payload) |
| `03_swagger.png` | FastAPI Swagger UI at `/docs` showing `POST /query` |

Convention: two-digit prefix + underscore + snake_case + `.png`. Capture from a browser window at standard width.
